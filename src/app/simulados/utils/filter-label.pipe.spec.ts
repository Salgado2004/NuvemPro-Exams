import { FilterLabelPipe } from './filter-label.pipe';

describe('FilterLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
